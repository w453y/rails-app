<p id="notice"><%= notice %></p>

<h1>Tasks</h1>
<style>
  th, td {
    text-align: center;
    margin: auto;
  }
</style>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Completed</th>
      <th>Notes</th>
      <th>Upload</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.description %></td>
        <td><%= task.completed %></td>
        <td><%= task.notes %></td>
        <td><%= image_tag task.upload, style: 'width: 500px' %></td>
        <td><%= link_to 'Show', task, class:"btn btn-primary text-white" %></td>
        <td><%= link_to 'Edit', edit_task_path(task), class: "btn btn-success text-white" %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger text-white" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="d-flex align-items-center justify-content-center">
  <%= link_to 'New Task', new_task_path, class: "btn btn-primary text-white" %>
</div>

<div class="contianer mt-5">
  <div class="row">
    <div class="col-md-12">
      <div class="d-flex align-items-center justify-content-center">
        <%= image_tag("w453y.jpeg") %>
      </div>
    </div>
  </div>
</div>